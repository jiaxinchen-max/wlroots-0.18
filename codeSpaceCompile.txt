vscode:
git add .;git commit -m 'bug fix';git push

/workspace:
mkdir -p /tmp/.termux-build;
sudo service apache2 start;
cd wlroots/;git pull;cd ..;tar -zcf wlroots.tar.gz wlroots/;sha256sum wlroots.tar.gz;sudo mv wlroots.tar.gz /var/www/html/termux-packages/

/workspace/termux-package:
./build-package.sh -s wlroots